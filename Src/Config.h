#pragma once

namespace EternaLimits
{
	// number of max allowed classes to be exposed to Lua
	const static unsigned int maxRegisteredClasses = 50;

	// number of max allowed functions per class to be exposed to Lua
	const static unsigned int maxRegisteredFunctions = 50;
}
